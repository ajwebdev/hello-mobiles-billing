import React from "react";
import { Button, View, Text } from "react-native";

const ReportScreen = ({ navigation }, props: any) => {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>Report</Text>
    </View>
  );
}

export default ReportScreen;
